{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npx puppeteer browsers install chrome@latest"
  },
  "deploy": {
    "startCommand": "npx puppeteer browsers install chrome@latest && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
